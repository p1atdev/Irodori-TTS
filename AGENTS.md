# IrodoriTTS + Character Reference

IrodoriTTS の基本機能 (README.md 参照) に加えて、画像を参照して生成する機能を実装する。

## Character Reference

現状、Voice Design という機能で、テキストによって声のスタイルを指定することができるが、これと似たような形で、画像を参照として声のスタイルを決定する機能を実装。

### アーキテクチャ

Voice Design と似たような形で、新たに `character` の条件を加えられるようにする。
Voice Design は `caption` の条件で attention などに追加条件を行っている。
具体的な実装はコードを見ること。

### 画像エンコーダー

画像エンコーダーとして何が適切かはまだわからないので timm を用いてモデルを柔軟に切り替えられるようにする。

```py
import timm

model = timm.create_model("hf_hub:SmilingWolf/wd-eva02-large-tagger-v3", pretrained=True)
```

みたいな学習済みモデルを使えるようにする。

### 画像プロジェクター

音声生成モデルと画像エンコーダーは学習済みモデルを使用することから、間にプロジェクターが必要になる。プロジェクターの形式も色々切り替えて試せるようにしたい。例えば、

- MLP
- Transformer
- Resampler
- 領域ごとにpooling

などを試せるようにする。とりあえずはシンプルな MLP を実装するが、将来のためにそこを簡単に切り替えられるように実装する。

### 画像前処理

基本的な前処理はデータセットの加工時に行うので複雑なことはしなくて良いが、シンプルな augumentation ができるようにしたい。
例えば、

- 軽い変形
- 軽い色の調整
- 軽いリサイズ
- (RGBA だった場合は) ランダムな背景色をつけたり

など。学習時に設定を指定できるようにしたい。

### データセット

データセットも新たに画像参照できるようにする必要がある。

## Previewing

現状、学習ループの中には検証ステップは存在するが、実際に生成結果を確認する機能が追加されていない。

検証ロジックと同様に、一定のステップごとに指定されたプレビュー用設定をもとに、プレビュー生成をして wandb に記録する機能を作成する。

プレビュー用設定には、生成するテキスト内容やその生成パラメータ、参照音声・参照キャプション・参照画像などのファイルパスを指定できるようにする。

## Python

python3 は使用できない。
uv を使う。フォーマッタは ruff を使う。
